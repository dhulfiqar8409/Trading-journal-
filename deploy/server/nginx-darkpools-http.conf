# Darkpools: rate-limit zone, http context. Installed as /etc/nginx/conf.d/darkpools.conf.
# Only the darkpools server block references it; other applications are unaffected.
limit_req_zone $binary_remote_addr zone=darkpools_login:1m rate=5r/m;
